# v1.0.1 — transposon-genome-ecology (2026-09-11)

**Read this cold.** v1.0.0 shipped and was audited by a 10-judge review panel; every item
below was then verified by the coordinator against the released tree at `3400098`. You
are the executor; the coordinator owns every remote operation. Branch `release/1.0.1-rc`
from the current `master`. Node 22 is required (`.nvmrc`); a user-level Node 22 exists on
this machine — `RELEASE-1.0.md` §1 records how it was found. Push ONLY that branch.

## Scope allowlist — read twice

You MAY correct statements, counts, comments, links, metadata, and documentation
structure; add a link and a sentence to the toy page; add tests that guard a shipped
claim. You MAY NOT change `sim/` behaviour, `defaultParams`, `TOY_DEFAULTS`, any
constant, any pre-registration, or re-run any experiment. **A fix changes what is SAID,
never what was MEASURED.**

## Punch list

### A. Claim-level

1. `README.md:69` "The validation surface is seven guards" with a 7-row table;
   `tests/guards/` holds **nine** (`domestication.test.ts` = GUARD 8,
   `tolerance.test.ts` = GUARD 9), and README:33 / FINDINGS:218 cite "guard 8's arm".
   Make the table nine rows (one line each on what 8 and 9 assert and do not claim).
   Fix the same count at `docs/RELEASE-1.0.md:596`; leave `CHANGELOG.md:113,146` (the v1
   entry was true when written) but add "(nine by v1.0)" in parentheses.
2. `README.md:12` "Two of them falsified the thing they were testing" — the table at
   :91-97 bolds only 002. Count one way: "one registered primary was falsified (002);
   registered secondaries were falsified in 003, 004 and 005, and 004's third secondary
   was later retracted as unresolvable" — and make the table's bolding match.
3. `docs/FINDINGS.md:114` "control up to `phi = 0.016`, four doublings above 003's first
   nonzero grid step of 0.125" — 0.016 is **below** 0.125, by three halvings
   (0.125 → 0.0625 → 0.031 → 0.016). Fix direction and count.
4. The toy page `web/index.html` has no link to FINDINGS, the repository, or a
   one-sentence "what this is". Add a small header line: what it is · link to
   `docs/FINDINGS.md` on GitHub · link to the repo · licence. Restore the qualifier in the
   legend at `:353-363`: the persistence threshold "0.03–0.075" was **measured on guard
   8's arm** (`beta = 0.1, pDom = 0.2`), not a model property. Guard 7 and the smoke tests
   must stay green (the page's model/state hash must not move; a header line is DOM only).
5. `sim/silencing.ts:46-49` quotes 0.67 → 15.97 ms / 24× and cites `web/main.ts`, which
   holds 1.06 → 35.73 ms / 34.7×. Replace with the current numbers or cite the commit the
   old numbers came from.

### B. Reproduce

6. `README.md:99-103` Setup: add Node ≥ 22 as the first line with the one-line reason
   (Node 18 crashes vitest/vite on `styleText`); use `npx playwright install --with-deps
chromium` and say `--with-deps` is Linux-only; state Playwright is for tests, not for
   `npm run build`/`preview`. FINDINGS "Reproducing" (`:279-319`): add Playwright; add
   the R line (R 4.3.3, ggplot2 4.0.2, patchwork 1.2.0, png 0.1-8; run from repo root);
   beside `npx tsx experiments/005-…` write the wall time (~6 h sharded / ~16 h single) and
   that it overwrites the committed CSV — "do not run this to check the figures".
7. `CHANGELOG.md:70` `## Unreleased` sits under `## [1.0.0]`. Move it above, or fold its
   content into 1.0.0 if it shipped (the repertoire-search timings README:63 quotes did).

### C. Hygiene / metadata

8. `README.md:145` "Mirrors `_pm/`…" → "Layout:"; `:162` delete `_scratch/`.
9. `docs/REFERENCES.md:3` "38 works / 42 DOIs" and `README.md:166` "38 works": re-tally
   the document (the audit counted ~50 DOI-bearing works; `docs/dois.txt` has 51 across
   all docs) and write the true numbers with the date of the tally.
10. `sim/params.ts:59` "overrides ten of them" — 12 of 17 `TOY_DEFAULTS` keys differ.
11. `CITATION.cff`: `license-url` for both licences (currently MIT only). Keep
    `date-released: 2026-09-10` (the tag was cut 2026-09-10 EDT).
12. `web/hash-harness.html` publishes to Pages as a visitor-reachable URL: add
    `<meta name="robots" content="noindex">` and a one-line "test fixture" note in its
    body; do not remove it (guard 7 uses it).
13. `docs/RELEASE-1.0.md`: top banner "Release evidence log, not a findings document."
14. `CHANGELOG.md`: `## [1.0.1] — 2026-09-11` listing every item above.

## Acceptance (record in `docs/RELEASE-1.0.1.md`)

- `npm test` 198/198 (+ any new), `npm run typecheck`, `npm run build` under Node 22;
  guard 7's state hash unchanged
- clean-clone: `git clone` → `npm ci` → `npx playwright install --with-deps chromium` →
  `npm test` → `npm run build`; serve `dist/` and smoke: 200, 0 console errors, the new
  header links resolve
- ghostcite on `docs/dois.txt` clean; gitleaks history + `dist/` clean
- one fresh-critic pass on the rendered toy + FINDINGS; non-waivable = a count/number
  disagreeing with the tree, a missing link, an unqualified threshold claim
- handoff: RC SHA, per-item disposition table, evidence

Push `release/1.0.1-rc` and stop.
